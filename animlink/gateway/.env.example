# AnimaLink Gateway — Environment Template
# Copy to .env and fill in values

# Admin API Key (required — set a strong random value)
GATEWAY_ADMIN_KEY=changeme

# Flask secret key
SECRET_KEY=change-me-in-prod
